---
name: df-sync
description: Sync data between local DevForge and server Portal
---

## DevForge Sync

Trigger bidirectional sync between local SQLite and server PostgreSQL.

1. Use MCP tool `devforge_sync` with direction = "both"
2. Report: pushed X projects, X issues, X notes; pulled X feedback, X comments
